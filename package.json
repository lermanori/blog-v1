{
  "name": "blog-v1",
  "version": "1.0.0",
  "description": "My priceless Nuxt.js project",
  "author": "lermanori",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate",
    "build:gh-pages": "cross-env DEPLOY_ENV=GH_PAGES nuxt build",
    "generate:gh-pages": "cross-env DEPLOY_ENV=GH_PAGES nuxt generate && rm docs && ren dist docs",
    "deploy": "git add . && git commit -m 'commit' && git push"
  },
  "dependencies": {
    "@neneos/nuxt-animate.css": "0.0.1",
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/vuetify": "0.5.5",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9",
    "push-dir": "^0.4.1",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2"
  }
}
